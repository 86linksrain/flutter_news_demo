import 'package:flutter/material.dart';

class DiscoverPage1 extends StatelessWidget {
  const DiscoverPage1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("发现详情"),
      ),
    );
  }
}
